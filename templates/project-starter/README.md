# {{PROJECT_NAME}}

Started with **AI Development OS v1.0**.

## Start (2 steps)

1. Open this folder in **Grok** (`cd` here, then `grok`) or **Antigravity**
2. In chat, type:

```text
start
```

The agent will ask you questions one at a time (platforms, API, MVP scope, …) and write `CONTEXT.md` for you.

When it summarizes, reply **`yes`**. Answer **A / B / C** when asked. Say **Start coding** before any code.

## Files (agent creates)

| Path | Purpose |
|------|---------|
| `CONTEXT.md` | Domain glossary (created during kickoff) |
| `docs/OPEN-QUESTIONS.md` | Deferred questions (if any) |
| `work/` | OS artifacts (intake, discovery, prd, …) |
| `AGENTS.md` | Agent instructions (auto-read) |
| `ai-dev-os.yaml` | OS paths and work_id |

## OS home

`{{AI_DEV_OS_HOME}}`