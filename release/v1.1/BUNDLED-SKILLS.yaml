# AI Dev OS — bundled skills (frozen snapshot v1.1.0)
# Live SSOT: skills/MANIFEST.yaml
# install-cli.sh symlinks to ~/.grok/skills/ and ~/.gemini/config/skills/ (grok + agy).
# Agents MUST load: $AI_DEV_OS_HOME/skills/<name>/SKILL.md

version: "1.1"

required:
  # OS bind + kickoff
  - setup-ads
  - setup-project-agents
  - setup-task-run
  - grill-me
  - grill-with-docs
  # Planning + issues
  - plan-to-issue-v2
  - grill-for-planning
  - plan-synthesis
  - plan-review
  - to-issues
  - to-prd
  # AFK execution
  - task-run
  - work-to-pr-v2
  - issue-processor
  - issue-spec-review
  - pr-readiness-check
  - tdd
  # Bug path
  - triage
  - diagnose