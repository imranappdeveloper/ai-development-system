# wf-docs-alpha

Sample API service for documentation planning fixtures.

## Quickstart (stale — planned update per DOC-PLAN)

```bash
go run ./cmd/server
```

> **Note:** Auth and API details are incomplete — see pending `docs/api.md` creation in DOC-PLAN.